# Analysis

The Pandas analysis module currently lives in `backend/app/analysis.py` so that the API can use it directly.

It reports question counts by section and topic, topic frequencies by year, difficulty distributions, question-type distributions, and topic trends.
